# 기여 가이드

> 문서 상태: CURRENT
> 기준일: 2026-08-31

이 프로젝트는 사람과 AI가 함께 작업하되, 변경의 의도·검증·책임을 저장소에 남기는 것을 원칙으로 한다.

## 시작하기

```powershell
pnpm setup
pnpm dev
```

작업 전 읽기 순서는 다음과 같다.

1. `docs/README.md`
2. `docs/PROJECT_CHARTER.md`
3. `docs/GAME_RULES.md`
4. `docs/DEVELOPMENT_PROCESS.md`
5. 변경 영역의 상세 명세
6. `docs/REVIEW_REQUIRED.md`

## 변경 시작

- 문서만 고치는 명백한 오탈자를 제외하면 `docs/templates/CHANGE_BRIEF.md` 형식으로 변경 계약을 작성한다.
- 한 작업은 하나의 사용자 가치 또는 하나의 결함에 집중한다.
- 범위 밖 개선은 현재 변경에 섞지 않고 백로그 또는 결정 대기 목록에 남긴다.
- 에이전트가 만드는 브랜치는 기본적으로 `codex/<type>-<topic>`을 사용한다.

권장 변경 유형은 `feature`, `fix`, `docs`, `test`, `refactor`, `chore`, `asset`, `ops`다.

## 구현과 커밋

- 서버가 시간·정답·점수·승패의 최종 권한을 가진다는 원칙을 유지한다.
- 공유 타입을 바꾸면 게임 코어, 서버, 웹, 저장 형식과 테스트 영향을 함께 확인한다.
- DB 마이그레이션은 전진 호환을 기본으로 하며 롤백·복구 방법이 없으면 병합하지 않는다.
- 생성 자산은 출처·권리·버전·해시·정답 영역·육안 검수 기록이 있어야 한다.
- 커밋에는 무엇을 바꿨는지보다 왜 바꿨는지가 드러나야 한다.
- 리팩터링과 동작 변경은 가능하면 별도 커밋으로 분리한다.

## 리뷰 요청 조건

리뷰 전 다음을 만족해야 한다.

- 변경 계약의 완료 조건 충족
- 관련 정본과 파생 문서 동기화
- `docs/QUALITY_GATES.md`의 필수 검증 통과
- 미실행 검증과 남은 위험 명시
- 사용자 데이터·보안·라이선스·운영 영향 명시
- 롤백 또는 기능 비활성화 경로 명시

게임 규칙, DB, 보안, 배포, 라이선스 변경은 작성자 외 사람의 승인이 필요하다.

## 완료 보고

다음 형식을 사용한다.

1. 결과 요약
2. 변경한 파일과 동작
3. 실행한 검증 및 결과
4. 실행하지 못한 검증
5. 남은 위험·결정 필요 사항
6. 배포·롤백 방법
