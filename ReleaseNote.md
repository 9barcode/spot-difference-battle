# 변경 이력

이 프로젝트의 주요 변경 사항을 기록합니다.

버전 번호는 [Semantic Versioning](https://semver.org/lang/ko/) 형식인 `주버전.기능.수정`을 사용합니다.

과거 버전은 당시 구현을 설명하는 역사 기록이며 규칙 문서가 아닙니다. 현재 게임 규칙의 유일한 Markdown 정본은 `docs/GAME_RULES.md`이고, `docs/MVP_DECISIONS.md`는 결정 근거만 기록합니다.

## [Unreleased]

### 추가

- 서버가 선택한 장면 ID와 웹 이미지·객체 마스크를 연결하는 다중 장면 카탈로그 기반
- 사용자 제공 카툰 연구실 후보 장면과 18개 객체 마스크
- 장면 ID별 서버 원본 이미지 카탈로그와 제출 이미지 좌표 검증
- 등록되지 않은 객체·중복 객체·허용되지 않은 효과의 서버 제출 차단
- 제작부터 풀이까지 혼자 확인할 수 있는 1인 테스트 모드
- 고양이·화분·공·창문·구름·베개·소파·전등 등 16개 수동 객체 마스크
- 객체별 색상 변경, 가로 확대, 세로 확대, 줄무늬, 점무늬, 별 무늬, 윤곽 효과
- 제작자와 찾는 사람을 분리한 비대칭 온라인 경기 흐름
- 제작 중 찾는 사람에게 편집 그림과 정답 정보를 숨기는 비공개 대기 화면

- PostgreSQL 기반 경기 결과 및 플레이어별 진행 기록 저장
- 서버 재시작 후 게스트 세션과 진행 중 경기 상태 복구
- 실제 PostgreSQL을 사용하는 서버 재시작 복구 통합 테스트
- 종료되거나 취소된 경기의 문제 신고 접수
- 서버 오류로 취소된 경기의 사유 표시 및 전적 제외 처리
- 로컬 개발용 PostgreSQL Docker Compose 구성과 데이터베이스 마이그레이션 명령
- 오래된 경기 상태 입력 차단과 추측·힌트 연속 입력 제한
- 중복 클라이언트 액션과 지연·중복 서버 스냅샷 회귀 테스트
- 독립 브라우저 두 개로 매칭·준비·기권 결과를 검증하는 Playwright E2E 테스트
- 제작 화면 비공개와 객체별 독립 선택을 검증하는 데스크톱·모바일 E2E 테스트
- 연구실 장면 18개 객체의 모바일 개별 터치 선택 회귀 테스트
- Chromium·Firefox·WebKit 브라우저 호환성과 키보드 포커스 E2E 테스트

### 변경

- 픽셀 색상 범위 선택 방식 대신 고정 객체 마스크 기반 편집 방식으로 변경
- 제작자인 사용자의 프로젝트 사용 허가에 따라 카툰 연구실 라이선스 상태를 `VERIFIED`로 변경
- 제작자가 `수정 완료`를 누르는 즉시 찾는 사람의 풀이 단계가 시작되도록 경기 상태 전환 변경
- 최신 객체별 차이점 편집 및 오답 누적 페널티 흐름에 경기 저장·신고 기능 통합
- 핵심 Markdown 전체를 현재 비대칭 게임 구현에 맞추고 `GAME_RULES.md`를 유일한 규칙 정본으로 통합
- Windows PowerShell 기준 로컬 실행·PostgreSQL 명령과 pnpm workspace 안내를 통일
- 구현된 테스트 범위와 출시 전 미완료 검증을 백로그에서 분리
- 연구실 객체 마스크를 실제 렌더링에 맞게 보정하고 모바일 터치 여유 영역을 효과 마스크와 분리

### 수정

- `pnpm check`와 `pnpm test` 실행 전에 내부 공유 패키지를 자동 빌드하도록 실행 순서 보완
- 서버 통합 테스트가 실제 PNG 문제 이미지와 정답 좌표 검증 경로를 사용하도록 fixture 수정
- 상위 폴더의 Vite 설정이 Vitest 실행에 섞이지 않도록 저장소 전용 테스트 설정 추가
- 웹 클라이언트가 현재 경기의 동일하거나 낮은 버전 스냅샷을 무시하도록 상태 역행 방지
- 닉네임 입력의 접근 가능한 이름과 공통 키보드 포커스 표시 추가

### 추가 예정

- 라이선스가 확인된 게임 장면 10개
- 완료 문제 이미지·정답의 신고 감사용 보관
- 운영 로그·지표·모니터링과 스테이징 배포
- 랭킹, 친구 대전, 관전 기능
- 계정 및 사용자용 경기 기록 조회

## [0.1.1] - 2026-07-22

### 수정

- 이미 찾은 정답을 다시 눌러도 오답 페널티가 적용되지 않도록 수정

## [0.1.0] - 2026-07-17

### 추가

- 실시간 1대1 매칭 및 준비 시스템
- 플레이어별 차이점 3개 제작 및 제출
- 상대가 제작한 차이점 실시간 교환
- 서버 중심 정답 판정
- 제작 30초 및 찾기 60초 제한시간
- 오답마다 3초씩 누적 시간 차감
- 게임당 힌트 1회
- 경기 이탈 및 재접속 대기 처리
- 승리, 패배, 무승부 및 기권 결과 화면
- 따뜻한 거실과 귀여운 고양이 2D 게임 장면
- 그림 영역 클릭 및 외곽선 하이라이트
- 선택한 영역의 색상 교체
- 나무 팔레트 UI와 빨강·주황·노랑·초록·파랑·남색·보라 7색
- 차이점 저장, 현재 작업 삭제 및 제출 기능
- PC와 모바일 대응 화면

### 변경

- 편집 화면에서 게임 그림이 더 크게 표시되도록 레이아웃 개선
- 색상 도구가 그림 위치를 밀어내지 않도록 팔레트를 그림 위에 배치
- 오답 후 동일한 수정 그림을 다시 계산하지 않도록 렌더링 최적화
- 제작시간 종료 시 기본 차이점을 삽입하지 않고 시간초과로 처리

### 제거

- 자유 연필 및 지우개 편집 기능
- 포토샵형 전체 색상 피커
- 제한시간 종료 시 표시되던 임시 별·물음표·색상 원

### 검증

- TypeScript 타입 검사
- 전체 프로덕션 빌드
- 게임 코어 단위 테스트
- Socket.IO 서버 통합 테스트
- 반복 오답 누적 차감 및 오답 후 힌트 사용 테스트
