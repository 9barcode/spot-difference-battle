# 변경 요약 — 게임 규칙 정본 단일화 (2026-07-30)

> 문서 상태: HISTORICAL
> 현재 규칙은 유일한 Markdown 정본 `GAME_RULES.md`를 따른다.

## 확인 결과

- `GAME_RULES.md`의 역할·수치·시간·승패·기권·공개 범위를 공유 `GAME_CONFIG`와 서버 `GameMatch`에 대조했다.
- 현재 규칙값은 차이점 3개, 제작 30초, 찾기 60초, 오답 3초 차감, 힌트 1회, 재접속 유예 10초, 자동 보충 시점 3초 전으로 일치한다.
- CURRENT 문서에서 구형 대칭 제작, 차이점 5개, 점수 비교와 정상 무승부를 현재 규칙으로 사용하는 내용은 발견되지 않았다.
- 구형 표현은 비활성 UI 시안 또는 HISTORICAL 변경 기록으로만 남아 있으며 현재 구현 기준이 아님을 표시한다.

## 문서 구조 개선

- 게임 규칙을 정의하는 Markdown 정본을 `GAME_RULES.md` 하나로 확정했다.
- `MVP_DECISIONS.md`는 결정 근거, `GAME_STATE.md`는 상태 매핑으로 역할을 분리했다.
- README, 사용자 흐름, 화면·기술·테스트·백로그·UI 점검 문서는 정본을 요약하거나 구현 관점으로 매핑한 파생 문서임을 명시했다.
- 문서 충돌은 우선순위 선택으로 덮지 않고 정본·코드·파생 문서·테스트를 함께 고치는 결함으로 처리하도록 운영 기준을 바꿨다.
