# 문서 운영 기준 — 단일 규칙 정본

> 문서 상태: CURRENT
> 기준일: 2026-08-25

## 단일 규칙 정본

게임의 역할, 행동 권한, 수치, 시간, 승패, 기권, 공개 범위와 신고 조건을 규정하는 Markdown 정본은 `GAME_RULES.md` 하나뿐이다.

- 공유 `GAME_CONFIG`는 정본의 수치를 실행하는 코드다.
- 상태·화면·흐름 문서는 정본을 각 관점으로 매핑한다.
- `MVP_DECISIONS.md`는 결정 이유와 변경 이력을 보존한다.
- 기술·테스트·백로그 문서는 구현 계약과 작업 상태를 설명한다.
- `CHANGES_*.md`와 과거 릴리스는 역사 기록이다.

2026-08-04 동시 빨리찾기 구현과 테스트 전환이 완료되어 `GAME_RULES.md`와 파생 문서를 `CURRENT`로 승격했다.
2026-08-22 문제별 실제 차이 수, 점수, 완주 시간 가산점과 양쪽 완주 즉시 종료를 구현하고 2026-08-25 현재형 파생 문서를 다시 동기화했다.
2026-08-27 첫 전체 완주자를 즉시 승자로 확정하고 양쪽에 결과를 동기화하는 규칙으로 전환했다.

## 문서 상태

- `CURRENT`: 현재 구현·확정 결정의 기준
- `PROPOSED`: 확정됐지만 아직 구현되지 않은 목표
- `TRANSITION`: 현재와 다음 상태를 함께 설명해야 하는 문서
- `HISTORICAL`: 당시 변경 기록
- `ARCHIVED`: 더 이상 사용하지 않는 문서

## 문서별 책임

| 문서                                        | 책임                            |
| ------------------------------------------- | ------------------------------- |
| `GAME_RULES.md`                             | 게임 규칙의 유일한 정본         |
| `MVP_DECISIONS.md`                          | 결정 이유와 변경 이력           |
| `GAME_STATE.md`                             | 서버 상태와 입력 권한           |
| `GAME_DESIGN.md`                            | 핵심 경험과 제품 목표           |
| `USER_FLOW.md`, `SCREEN_SPEC.md`            | 사용자 흐름과 화면 요구         |
| `TECH_SPEC.md`                              | 기술 계약과 마이그레이션        |
| `TEST_PLAN.md`, `IMPLEMENTATION_BACKLOG.md` | 검증 범위와 작업 상태           |
| `GAME_ASSETS.md`                            | 문제 이미지 쌍 품질과 등록 기준 |
| `README.md`                                 | 입문용 요약과 실행 방법         |

## 규칙 변경 절차

1. 결정 이유를 `MVP_DECISIONS.md`에 기록한다.
2. `GAME_RULES.md`를 `PROPOSED`로 작성한다.
3. 상태·기획·흐름·화면·기술·테스트·에셋·백로그를 함께 정합화한다.
4. 구형 표현과 숨은 규칙을 전체 검색한다.
5. 코드와 데이터 마이그레이션을 구현한다.
6. 타입·단위·통합·브라우저 테스트를 통과한다.
7. 같은 변경에서 정본과 파생 문서를 `CURRENT`로 승격한다.

파생 문서나 코드가 정본과 다르면 어느 하나를 임의로 우선하지 않고 결함으로 처리한다.

## 역사 문서

날짜별 변경 요약은 당시 문제와 해결 과정을 보존한다. 현재 구현과 달라도 내용을 현재형으로 덮어쓰지 않고 새 변경 기록에서 대체 사실을 남긴다.
