# 변경 요약 — 구현·문서 단일 기준 정리 (2026-07-29)

> 문서 상태: HISTORICAL
> 현재 규칙은 `GAME_RULES.md`, 남은 작업은 `IMPLEMENTATION_BACKLOG.md`를 따른다.

## 오늘 확인

- 원격 `main`과 현재 브랜치의 시작 커밋이 동일함을 확인했다.
- 2026-07-29 원격 신규 커밋과 다른 작업자의 겹치는 변경이 없음을 확인했다.
- 현재 작업 트리는 다중 장면 카탈로그 기반과 관련 문서 정합화로 구성된다.

## 다중 장면 기반

- 공유 장면 ID와 기본 장면 타입을 추가했다.
- 서버가 등록 장면 ID를 경기 생성 시 선택하고 저장·복구하도록 연결했다.
- 웹 이미지·객체 마스크·자동 보충 후보·라이선스를 장면 카탈로그로 묶었다.
- 편집기·합성·온라인·1인 모드가 선택된 장면 정의를 사용하도록 정리했다.
- 장면 카탈로그와 서버 선택·복구 회귀 테스트를 추가했다.

현재 실제 장면은 라이선스가 확인되지 않은 `prototype-room` 한 개다. 장면별 서버 원본과 객체 허용 목록 검증은 출시 차단 백로그다.

## 문서 정합화

- 비대칭 제작자/찾는 사람 구조를 공식 게임 규칙으로 확정했다.
- 구형 양쪽 제작·동시 풀이·동점·재대전 표현을 현재 문서에서 제거했다.
- 실제 구현과 목표 기술 구조, 자동화 완료와 미검증 범위를 구분했다.
- 활성 `MvpApp`과 비활성 Figma 시안을 구분했다.
- PowerShell·pnpm 기준 실행 안내와 에셋 출처 상태를 바로잡았다.
- 문서 상태와 우선순위를 `DOCUMENTATION.md`에 추가했다.
- 과거 날짜별 변경 요약을 `HISTORICAL`로 표시했다.

## 검증

- `pnpm check`
- `pnpm build`
- `pnpm test` — 39개 통과, PostgreSQL 환경 필요 테스트 1개 건너뜀
- `pnpm e2e` — Chromium·Firefox·WebKit 9개 통과
- Markdown 상대 링크·후행 공백·구형 규칙 표현 검사
