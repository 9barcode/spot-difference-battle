# AI 작업 기준

이 파일은 이 저장소를 수정하는 모든 AI 코딩 도구와 자동화 에이전트에 적용한다.

## 작업 시작 전 필수 확인

1. `README.md`와 `docs/README.md`를 읽는다.
2. 제품 범위는 `docs/PROJECT_CHARTER.md`, 게임 동작과 수치는 `docs/GAME_RULES.md`를 따른다.
3. `docs/REVIEW_REQUIRED.md`에서 미확정 결정을 확인한다.
4. 변경 대상에 해당하는 화면·기술·테스트·에셋 문서를 읽는다.
5. 작업 트리와 현재 브랜치를 확인하고 기존 변경을 덮어쓰지 않는다.

문서와 코드가 다르면 임의로 하나를 정답으로 선택하지 않는다. 출시 차단 결함으로 기록하고, 의도 확인이 필요한 경우 `docs/REVIEW_REQUIRED.md`에 추가한다.

## 필수 작업 절차

모든 변경은 `docs/DEVELOPMENT_PROCESS.md`의 단계를 따른다.

- 작은 오탈자 외에는 변경 목적, 범위, 제외 범위, 위험, 검증 방법을 먼저 정의한다.
- 게임 규칙·상태·공유 계약·DB·배포 변경은 고위험 변경으로 취급한다.
- 요구사항은 `docs/TRACEABILITY.md`의 ID에 연결하거나 새 ID를 만든다.
- 코드 변경과 관련 문서·테스트 변경을 같은 작업에서 완료한다.
- 실행하지 않은 검증을 통과했다고 쓰지 않는다.
- 완료 보고에는 변경 파일, 실행한 명령, 결과, 미검증 항목, 롤백 방법을 포함한다.

## 사람 승인 없이 변경하지 않는 항목

- 확정 게임 목표, 점수, 승패, 시간, 난이도 또는 정보 공개 규칙
- 파괴적 DB 마이그레이션과 저장 데이터 삭제
- 라이선스·권리 상태를 `VERIFIED`로 승격하는 작업
- 운영 배포, 비밀정보, 비용이 발생하는 외부 서비스 설정
- 역사 문서 삭제 또는 기존 결정 기록의 의미 변경
- `docs/REVIEW_REQUIRED.md`에서 사람 결정을 요구하는 항목의 임의 확정

## 기본 검증

변경 유형별 필수 명령은 `docs/QUALITY_GATES.md`가 정본이다. 코드 변경의 기본 세트는 다음과 같다.

```powershell
pnpm check
pnpm test
pnpm build
```

게임 흐름, 브라우저 상호작용, 자산 또는 배포 변경은 관련 E2E·PostgreSQL·Docker 검증을 추가한다.

## 문서 규칙

- 현재 사실은 `CURRENT` 문서에만 쓴다.
- 미래 목표는 현재 구현처럼 표현하지 않는다.
- `CHANGES_*.md`는 역사 기록이므로 현재형으로 고치지 않는다.
- 중복 수치는 가능한 한 정본 링크로 대체한다.
- 문서 기준일은 의미 있는 검토를 완료한 날에만 갱신한다.
